# KETI AI 스토리지 오케스트레이션 — 모듈별 고도화 포인트 기술 분석

본 문서는 `/root/workspace` 기준 구현을 바탕으로, **이미 존재하는 구조를 더 지능적으로 다듬을 수 있는 방향**을 정리한다. 미구현 항목 나열이 아니라, 현재 파이프라인·의사결정 계층을 유지한 채 발전시킬 수 있는 기술적 관점에 초점을 둔다.

---

## [1] 스케줄링 (StorageClass 선정 포함)

### 현재 구조 요약

- **StorageClass(SC)**: `ai-storage-webhook`이 PVC `CREATE` 시 메타데이터만으로 SC를 선정·주입한다. Pod 역조회 없이 PVC 라벨/어노테이션·요청 스펙을 사용한다. 명시적 `storage-tier`(또는 동등 키)가 있으면 후보 SC를 해당 티어로 제한한다. `ReplaceableStorageClasses` 등으로 API 서버 기본 SC가 채워진 경우 티어 정책 SC로 교체하는 경로가 있다.
- **커스텀 스케줄러** (`ai-storage-scheduler`): Kubernetes 기본 필터/스코어 플러그인 위에 KETI 전용 필터·스코어 플러그인을 얹는다. **APOLLO**가 gRPC로 **플러그인 가중치**(PPO 출력)와 노드 선호·워크로드 시그니처 등을 내려주면, 스코어 단계에서 KETI 플러그인 가중치를 동적으로 적용한다.
- **입력 데이터**: Pod/PVC 라벨·어노테이션, APOLLO `SchedulingPolicy`, 노드/캐시 정보, PVC 바인딩·존, Kueue·파이프라인 스테이지 메타, 노드 라벨 기반 스토리지 티어·I/O 특성 등.

### 현재 의사결정 방식

| 구분 | 방식 |
|------|------|
| **SC 선정** | (1) **규칙**: 티어 오버라이드, `accessModes` 지원 여부로 후보 필터. (2) **가점 합산형 스코어링**: 워크로드 유형(preprocess/train/infer), GPU 유무, 프레임워크 카테고리, 복제 수·PVC 개수·마운트 경로 등에 따라 SC별 점수를 부여하고 최고점 선택. 일부 메타가 없으면 해당 항목 0점. |
| **노드 스케줄링** | **필터(규칙/제약)** + **스코어(가중 합)**. KETI 플러그인은 CRD 기본 가중치 또는 **APOLLO가 준 0~1 가중치를 정수 가중치로 변환**해 반영한다. 최종 노드는 스코어 합으로 선택. |

### 단순하거나 제한적인 부분 (핵심 2~3개)

1. **SC 쪽 점수 체계가 고정 테이블에 가깝다.** 워크로드·프레임워크·GPU 조합에 대한 가점이 코드에 박혀 있어, 클러스터별 스토리지 실측(지연, 처리량)이나 시기별 부하와의 **동적 연동**은 아직 구조의 중심이 아니다. (코드에도 `cpu_request`/`memory_request` 기반 점수화·용량 필터는 후속 단계 TODO로 남아 있다.)
2. **스케줄러의 “지능”이 플러그인 가중치 조정에 집중된다.** 노드별 세부 비용은 각 스코어 플러그인 내부의 휴리스틱·라벨 매칭에 의존하고, APOLLO는 **어떤 플러그인을 더 믿을지**를 학습한다. 즉 **정책 레버는 넓지만, 노드 간 미세 비교는 플러그인 구현 품질에 묶인다.**
3. **스케줄링 단위가 Pod 단위 순차 처리에 가깝다.** 동시 다발적 워크로드에 대한 **공동 최적화(혼잡·다중 커모디티)** 는 프레임워크 차원에서 드러나지 않는다.

### 고도화 가능 방향 (구조 유지 전제)

- **SC 스코어**: 현재 테이블을 “정책 엔진이 조정 가능한 가중치·특성 맵”으로 두고, **동일 입력(메타데이터)에 대해** APOLLO·Forecaster·운영 규칙이 **가중치만 조정**하도록 하면 웹훅·스케줄러 분리를 유지한 채 학습·튜닝 여지를 키울 수 있다.
- **플러그인 내부**: `DataLocalityAware`·`StorageTierAware` 등이 이미 다차원 점수를 내므로, **정규화·캘리브레이션**(워크로드 유형별 스케일 맞추기)과 **불확실성 구간**(예측 구간이 넓을 때 보수적 선택)을 넣으면 APOLLO 가중치와의 **역할 분담**이 명확해진다.
- **관측 연동**: 스케줄 직전/직후 메트릭을 피드백에 이미 쓰는 패턴을 확장해, **보상·가중치**가 “클러스터 평균”뿐 아니라 **스토리지·큐 대기** 등 세분 신호를 받도록 점진적으로 확장할 수 있다.

---

## [2] 자원 사용량 예측 (insight-hub 포함)

### 현재 구조 요약

- **수집 경로**: **insight-trace** 사이드카가 워크로드 관측을 담당하고, **insight-scope**가 노드(및 선택적으로 Pod) 단위 스냅샷을 수집해 **Insight Hub** gRPC(`SubmitHistoryData`)로 넘긴다. Hub는 SQLite에 **시계열 스냅샷**을 저장한다.
- **Hub 스키마**: 노드명, 타임스탬프, `cpu_util`, `mem_util`, `gpu_util`, `storage_io_util`, (선택) Pod 네임스페이스·이름.
- **예측기**: `apollo/node-resource-forecaster`의 **LSTM**이 `input_dim=4`(CPU·Memory·GPU·Storage I/O), 시퀀스 길이 60, **다중 호라이즌**(5·10·30·60분) 예측을 수행한다. Hub 주소가 설정되면 **Hub에서 이력을 주기적으로 pull**해 학습·추론에 사용하는 동기화 경로가 있다.

### 현재 의사결정 방식

- **특징 벡터**: 노드(및 선택적 Pod) **이용률 스칼라 4종**이 핵심이다. 스토리지는 **“I/O utilization” 단일 채널**로 들어간다.
- **시계열 여부**: **시계열 LSTM**이며, 단일 시점이 아니라 과거 윈도우 기반 예측이다.
- **사용처**: APOLLO `GetSchedulingPolicy`에서 Forecaster 스냅샷을 받아 **PPO 상태 텐서** 구성 및 **스케줄링 결과 피드백 시 보상**(스케줄 전후 메트릭 차이 등)에 활용한다.

### 단순하거나 제한적인 부분 (핵심 2~3개)

1. **스토리지 신호가 1차원 utilization에 가깝다.** 대역폭·지연·IOPS·계층(L0/L1/L2)별 분리, PVC/노드별 병목 위치는 Hub 기본 스키마 한 줄로는 표현되지 않는다. “스토리지를 고려한다”와 “워크로드별 I/O 패턴을 예측한다”는 **다른 난이도**다.
2. **Hub → 정책 → 오케스트레이션**으로 이어지는 **폐로가 스케줄링 쪽에 치우쳐 있다.** Forecaster·Hub 데이터가 **마이그레이션 시점 선택·캐시 티어**까지 동일 밀도로 들어가 있다고 보기 어렵다(연동 포인트는 확장 가능).
3. **Pod 단위 이력**은 수집 경로가 있으나, **학습 목표**(노드 전체 vs 워크로드 시그니처별 헤드)가 모델·보상 설계와 **끝까지 정렬**되어야 지능이 누적된다.

### 고도화 가능 방향 (구조 유지 전제)

- **동일 LSTM/시계열 골격**에 **입력 채널 확장**(예: 스토리지 세분·큐 길이·네트워크 지표)을 **선택적 피처**로 추가하고, 기존 4채널은 하위 호환으로 유지.
- **호라이즌별 신뢰도**(이미 모델에 confidence·uncertainty 헤드가 있는 패턴)를 **스케줄러 가중치·SC 힌트**에 연결하면, 예측이 “숫자 하나”가 아니라 **의사결정 게이트**가 된다.
- **Hub 질의 API**(`GetNodeHistory` / `GetPodHistory`)를 활용해 **워크로드 ID·스테이지별** 재학습 배치를 분리하면, 현재 파이프라인을 깨지 않고 **세분화된 예측기**를 늘려 갈 수 있다.

---

## [3] 오케스트레이션 (정책 + 실행 구조)

### 현재 구조 요약

- **ai-storage-orchestrator**는 HTTP API로 **마이그레이션**, **오토스케일링**, **로드밸런싱(마이그레이션 플랜 실행)**, **프로비저닝**, **선점(preemption)**, **캐싱**(워밍업·티어 마이그레이션·정책 적용) 등을 **컨트롤러**로 실행한다. 상태는 주로 **인메모리**이며, 마이그레이션은 컨테이너 상태 분석 후 최적화 Pod 생성·원본 삭제의 **파이프라인**으로 동작한다.
- **정책 주입**: 캐싱에 한해 `POST /api/v1/caching/policy`로 **외부 정책 엔진의 결정**(액션·확률·대상 ID 등)을 받아 `CachingController.ApplyPolicyDecision`에서 분기한다. APOLLO README 상 **orchestration-policy-engine**(DQN/Actor-Critic 등)은 **스케줄링용 PPO와 별도 축**으로 기술되어 있다.
- **스케줄러 측 피드백**: APOLLO `ReportSchedulingResult`로 **스케줄 성공/실패·지속시간** 등이 RL 보상 학습에 들어간다.

### 현재 의사결정 방식

| 영역 | 방식 |
|------|------|
| **마이그레이션·스케일 등** | API 요청·컨트롤러 로직(규칙·휴리스틱) 중심; 타깃 노드 등은 호출자·내부 플랜에 따름. |
| **캐시 정책** | **외부 결정 JSON 적용** + 로컬 분기; ML은 오케스트레이터 내부가 아니라 **정책 엔진 쪽** 가정. |
| **스케줄링 정책** | APOLLO **PPO**가 플러그인 가중치 등을 생성. |

### 단순하거나 제한적인 부분 (핵심 2~3개)

1. **오케스트레이션 액션 결과 → 글로벌 정책 학습**으로 돌아가는 경로가 **스케줄링 RL**만큼 일관되게 노출되어 있지 않다. 마이그레이션·캐시 효과가 **동일 보상 함수**에 합류하기 어렵다.
2. **인메모리 상태**는 재시작 시 학습·감사 추적이 끊긴다. “지능”을 쌓으려면 **결과 로그·메트릭의 외부 저장**과의 연결이 중요하다.
3. **Gluesys 등 외부 스텁**과의 연동은 확장 포인트로 열려 있으나, **데이터 플레인 실측**과의 폐루프는 구성에 따라 달라진다.

### 고도화 가능 방향 (구조 유지 전제)

- **캐시 정책 API**를 유지한 채, 적용 후 **케이스 메트릭**(히트율·지연·이동 비용)을 **동일 APOLLO 또는 별도 버퍼**로 보내 **오케스트레이션 전용 보상**을 정의하면, 스케줄링 RL과 **역할 분리**하면서도 시스템 전체 목표를 맞출 수 있다.
- **마이그레이션 파이프라인**은 이미 “상태 분석 → 최소 컨테이너 이전”이라는 강한 구조가 있으므로, **언제 트리거할지**만 Forecaster·Hub 기반 **트리거 정책**(임계값·불확실성 구간)으로 얹으면 실행부를 건드리지 않고 고도화할 수 있다.
- **로드밸런싱이 마이그레이션 컨트롤러를 호출**하는 패턴은 유지하면서, 플랜 생성만 **비용 모델**(예측 기반)로 바꾸는 식의 **계층적 분리**가 가능하다.

---

## 요약 표

| 꼭지 | 한 줄 요약 |
|------|------------|
| **[1] 스케줄링·SC** | 웹훅은 메타데이터·고정 가점으로 SC 선택; 스케줄러는 필터+가중 스코어+APOLLO 플러그인 가중치. |
| **[2] 예측·Hub** | Hub에 4종 이용률 시계열; LSTM 다호라이즌; APOLLO 스케줄 정책·보상에 주로 사용. |
| **[3] 오케스트레이션** | API·컨트롤러 실행형; 캐시는 정책 주입 훅 존재; 스케줄링과의 RL 피드백 밀도는 비대칭. |

---

*문서 기준 경로: `ai-storage-webhook`, `ai-storage-scheduler`, `apollo/scheduling-policy-engine`, `apollo/node-resource-forecaster`, `insight-hub`, `insight-scope`, `ai-storage-orchestrator`.*
