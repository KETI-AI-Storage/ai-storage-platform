apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai-storage-workloads
resources:
  - job.yaml
images:
  - name: migration-test
    newName: ketidevit2/migration-test
    newTag: "latest"   # CI (image.yml) bumps this to the 7-char commit SHA on build
