---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: orchestrationpolicies.apollo.keti.re.kr
spec:
  group: apollo.keti.re.kr
  names:
    kind: OrchestrationPolicy
    listKind: OrchestrationPolicyList
    plural: orchestrationpolicies
    singular: orchestrationpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.policyType
      name: Type
      type: string
    - jsonPath: .spec.urgency
      name: Urgency
      type: string
    - jsonPath: .spec.probability
      name: Prob
      type: number
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.targetNode
      name: Target
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          OrchestrationPolicy KETI 오케스트레이션 정책 CRD
          Forecaster 예측 결과를 기반으로 Policy Engine이 생성
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OrchestrationPolicySpec 정책의 상세 내용
            properties:
              autoExecute:
                default: false
                description: 'AutoExecute 자동 실행 여부 (기본값: false)'
                type: boolean
              horizon:
                description: 'Horizon 예측 시간 범위 (분 단위, 예: 30 = 30분 후)'
                format: int32
                type: integer
              parameters:
                additionalProperties:
                  type: string
                description: Parameters 추가 파라미터 (오퍼레이터별 상세 설정)
                type: object
              policyType:
                description: PolicyType 정책 타입 (migration, scaling, provisioning 등)
                enum:
                - migration
                - scaling
                - provisioning
                - caching
                - loadbalance
                - preemption
                type: string
              priorityScore:
                description: PriorityScore 우선순위 점수 (높을수록 중요)
                format: int32
                maximum: 100
                minimum: 0
                type: integer
              probability:
                description: Probability 이 정책의 신뢰도 (0 ~ 100, 퍼센트)
                format: int32
                maximum: 100
                minimum: 0
                type: integer
              reason:
                description: Reason 정책 생성 이유
                type: string
              resourceType:
                description: ResourceType 주요 리소스 타입
                enum:
                - CPU
                - MEMORY
                - GPU
                - STORAGE_IO
                - NETWORK
                type: string
              sourceNode:
                description: SourceNode 소스 노드 (마이그레이션 출발지)
                type: string
              targetNamespace:
                description: TargetNamespace 대상 네임스페이스
                type: string
              targetNode:
                description: TargetNode 대상 노드 (마이그레이션 목적지, 프로비저닝 대상 등)
                type: string
              targetWorkload:
                description: TargetWorkload 대상 워크로드 (Pod/Deployment 이름)
                type: string
              urgency:
                description: Urgency 긴급도
                enum:
                - LOW
                - MEDIUM
                - HIGH
                - CRITICAL
                type: string
            required:
            - policyType
            - probability
            - reason
            - urgency
            type: object
          status:
            description: OrchestrationPolicyStatus 정책의 현재 실행 상태
            properties:
              completedAt:
                description: CompletedAt 실행 완료 시간
                format: date-time
                type: string
              conditions:
                description: Conditions 상태 조건들
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              executedAt:
                description: ExecutedAt 실행 시작 시간
                format: date-time
                type: string
              executedBy:
                description: ExecutedBy 실행한 오퍼레이터 이름
                type: string
              message:
                description: Message 상태 메시지
                type: string
              phase:
                description: Phase 현재 실행 단계
                enum:
                - Pending
                - Approved
                - Executing
                - Completed
                - Failed
                - Rejected
                type: string
              result:
                description: Result 실행 결과 상세
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
