---
apiVersion: v1
kind: Pod
metadata:
  name: tensorflow
  labels:
    run: tensorflow
    sidecar.istio.io/inject: "true"
spec:
  containers:
    - image: tensorflow/tensorflow:latest
      name: tensorflow




---
apiVersion: v1
kind: Pod
metadata:
  name: tensorflow
  labels:
    run: tensorflow
    sidecar.istio.io/inject: "true"
spec:
  containers:
    - image: tensorflow/tensorflow:latest
      name: tensorflow
